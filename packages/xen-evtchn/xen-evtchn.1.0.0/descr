Xen event channel bindings.

These allow your Unix process or Mirage kernel to exchange event channel
notifications with other VMs on the same host.

These are needed for building xen device drivers (e.g. mirage-block-xen)
