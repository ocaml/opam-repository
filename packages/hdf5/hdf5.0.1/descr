Manager the HDF5 file format used to store and organize large amounts of data.

The librarhy implements the functionality needed for reading and writing HDF5 file
format.  HDF5 is a file format used to store and organize large amounts of data.  Also
provided is a fast way of working with large arrays of records, much faster than OCaml
arrays of records.
