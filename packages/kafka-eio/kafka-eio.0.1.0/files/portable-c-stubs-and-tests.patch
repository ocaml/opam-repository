diff --git a/kafka-eio-core/lib/dune b/kafka-eio-core/lib/dune
index 0ef8c14..2a1e797 100644
--- a/kafka-eio-core/lib/dune
+++ b/kafka-eio-core/lib/dune
@@ -5,5 +5,6 @@
  (modules kafka_raw kafka_error kafka_consumer_handle kafka_security)
  (foreign_stubs
   (language c)
-  (names kafka_stubs))
- (c_library_flags (-lrdkafka)))
+  (names kafka_stubs)
+  (flags (:standard -I/usr/local/include -I/opt/homebrew/include)))
+ (c_library_flags (-L/usr/local/lib -L/opt/homebrew/lib -lrdkafka)))
diff --git a/kafka-eio-producer/test/test_producer_unit.ml b/kafka-eio-producer/test/test_producer_unit.ml
index be69f7d..d85e3a6 100644
--- a/kafka-eio-producer/test/test_producer_unit.ml
+++ b/kafka-eio-producer/test/test_producer_unit.ml
@@ -61,6 +61,8 @@ let test_close_resolves_pending_produce_await () =
    either pipe. Reproduced empirically: 5 create/close cycles on one
    still-open switch grew /proc/self/fd by +4 fds every cycle. *)
 let test_close_does_not_leak_pipe_fds () =
+  if not (Sys.file_exists "/proc/self/fd") then
+    Alcotest.skip ();
   Eio_main.run @@ fun _env ->
     Eio.Switch.run @@ fun sw ->
       let fd_count () = Array.length (Sys.readdir "/proc/self/fd") in
