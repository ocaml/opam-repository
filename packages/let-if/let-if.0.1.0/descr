This ppx brings a `let%if` construct similar Rust's `if let`.

A let-expression decorated with [%%if ] is like a regular let-expression except
`()` is returned if the bound expression doesn't match the pattern.
