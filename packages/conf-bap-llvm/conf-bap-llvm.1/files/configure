#!/bin/sh

config="llvm-config"

if ! which $config ; then
    config="llvm-config-mp-3.4"
fi

if ! which $config ; then
    exit 1
else
    version=`$config --version`
fi

cat > conf-bap-llvm.config <<EOF
config: "$config"
version: "$version"
EOF
