POSIX time and clocks
The time library is a small OCaml library providing types and functions for
using POSIX time and clocks. This library should work on all POSIX compliant
systems implementing the used functions (OS X as of 10.10 does not).

