Pure OCaml implementations of Mirage consoles, for Unix and Xen

The Unix version of the console currently uses standard output. The
Xen kernel version of the console uses the primary PV console ring.

There is also a Unix userspace utility which creates and services Xen
consoles.